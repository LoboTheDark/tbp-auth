spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tbp_user
    username: postgres
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
backend:
  syncSteamApiUrl: http://localhost:8081/api/games/loadGameList